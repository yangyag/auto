---
name: strategy-pipeline
description: 전략 로직, 수식, 계산식, 예산 분배, TP/리스크 규칙, 트리거 조건, 재고 비율 계산이 변경되는 모든 작업에서 반드시 사용한다. Planner → (Math Expert) → Generator → Evaluator 파이프라인을 강제하고, 수학적 검증이 필요한지 자동으로 판별한다. "전략 수정", "TP 변경", "리스크 룰", "예산 분배", "수식 추가", "계산 로직", "트리거 조건" 같은 표현이 등장하거나, 변경 범위에 수치 계산이 포함될 가능성이 보이면 즉시 이 skill을 발동시킨다. 단순 버그 수정, 오타 수정, 로그 메시지 변경, 문서 업데이트, 단순 리팩터링에는 사용하지 않는다.
---

# Strategy Pipeline

전략/수식/리스크 로직을 다루는 작업의 표준 작업 흐름이다. 실거래 부작용이 발생할 수 있는 영역이므로 검증 우선 원칙을 강제한다.

## ⚠️ Subagent 호출 메커니즘 (반드시 준수)

이 skill에서 "subagent로 분리" 라는 표현은 **반드시 Task 도구를 통한 실제 subagent 호출**을 의미한다. 다음을 절대 금지한다:

- ❌ 같은 세션 안에서 "이제 Math Expert 모드로 검토하겠습니다"라고 말하고 자기가 검증하는 것
- ❌ "Math Expert 관점에서 보면..." 같은 롤플레이로 검증을 흉내내는 것
- ❌ Math Expert/Generator/Evaluator 정의 파일을 컨텍스트로 읽고 그 관점을 흉내내는 것

올바른 호출 방법:

- ✅ Task 도구를 명시적으로 invoke해서 `subagent_type: math-expert`(또는 `generator`, `evaluator`)로 호출
- ✅ 별도의 fresh 컨텍스트에서 subagent가 작업하고, 최종 결과만 메인 세션으로 반환되는 구조
- ✅ 사용자에게 "Launching agent: math-expert" 같은 UI 알림이 보이는 게 정상

**왜 이게 중요한가**: 같은 컨텍스트에서 역할만 바꾸면 Math Expert가 Planner의 출력을 정당화하는 sunk cost가 그대로 작용한다. 컨텍스트 격리가 깨지면 검증의 의미 자체가 사라진다. 메인 세션이 자기 출력을 자기가 검증하는 것은 진짜 검증이 아니다.

호출이 막히는 환경(Task 도구 미사용 가능 등)이라면, 검증을 흉내내지 말고 사용자에게 **"subagent 호출이 불가능하므로 검증을 메인 세션이 수행함"** 이라고 명시적으로 알린다. 절대 분리된 척 하지 않는다.

## 적용 판별

이 파이프라인은 다음 작업에 적용한다:
- 전략 로직 변경
- 수식 또는 계산식 추가/변경
- 예산 분배 로직 변경
- TP(Take Profit)/SL/리스크 규칙 변경
- 진입/청산 트리거 조건 변경
- 재고 비율 또는 포지션 사이징 계산 변경
- 기준값(threshold) 변경

다음 작업에는 적용하지 않는다:
- 오타, 로그 메시지, 문서 수정
- 변수명 리팩터링, 임포트 정리
- 단순 패킷 핸들러 수정 같은 비계산 영역

## 기본 흐름

```
Planner → Generator → Evaluator
```

수식, 계산, 전략 로직, 예산 분배, TP/리스크 규칙 변경이 포함되면 흐름을 다음으로 바꾼다:

```
Planner → Math Expert → Generator → Evaluator
```

비단순 작업은 가능하면 각 역할을 실제로 분리된 subagent로 나눠 수행한다. 단순 작업이라도 같은 순서의 점검 흐름은 유지한다.

## Planner

작업의 시작점. 다음을 고정한 뒤에야 다음 단계로 넘어갈 수 있다.

- 요청을 기능 단위로 쪼개고 범위와 완료 기준을 먼저 고정한다.
- 영향 파일과 선행 인터페이스를 먼저 확인한다 (Read/Grep으로 실제 파일 확인 필수, 기억으로 추정 금지).
- 실거래 부작용 없는 검증 우선 원칙을 유지한다.
- 작업에 수식/계산/전략 로직/예산 분배/TP/리스크 규칙 변경이 들어가는지 먼저 판별한다.
- 해당되면 Math Expert 단계로 넘긴다. 해당되지 않으면 바로 Generator로 넘긴다.

Planner의 출력에는 반드시 다음이 포함되어야 한다:
1. 변경 범위 (파일 단위)
2. 완료 기준 (어떤 상태가 되면 끝났다고 볼 것인가)
3. Math Expert 검토 필요 여부 (yes/no, 근거 포함)
4. 영향받는 인터페이스 목록

## Math Expert

수학적 정합성을 검증하는 단계. **반드시 Task 도구로 `math-expert` subagent를 호출해야 한다.** 정의는 `agents/math-expert.md`를 따른다.

호출 방식:
- Task 도구 invoke → `subagent_type: math-expert`
- prompt에 Planner가 정리한 계획 + 검증할 수식/계산 명세를 명시적으로 전달
- 같은 세션 안에서 "Math Expert 관점으로 검토" 같은 롤플레이 금지

검증 대상:
- 수식, 계산식, 기준값
- 예산 분배 로직
- TP/SL/리스크 계산
- 트리거 조건
- 재고 비율 계산

검증 관점:
- 로직의 정합성
- 단위 일관성 (예: 수량 vs 금액, 비율 vs 절대값)
- 경계 조건 (0, 음수, 최대값, 빈 포지션)
- 계산 방향 (덧셈/뺄셈 부호, 분자/분모, 누적 vs 순간)
- 부동소수점 오차 영향

판정:
- 문제가 없으면 Generator 단계로 넘긴다.
- 수학적으로 틀리거나 불완전하면 즉시 Planner 단계로 되돌리고, 수정된 계획이 다시 Math Expert 검토를 거치게 한다.
- 절대 "대충 맞을 것 같다"로 통과시키지 않는다.

코드 스타일은 검증 대상이 아니다. 오직 로직의 수학적 정합성만 본다.

## Generator

구현 단계. **분리 기준에 해당되는 작업은 반드시 Task 도구로 `generator` subagent를 호출한다.** 정의는 `agents/generator.md`를 따른다.

호출 방식:
- Task 도구 invoke → `subagent_type: generator`
- prompt에 Planner가 고정한 범위/완료 기준/영향 인터페이스 + (해당 시) Math Expert APPROVED 명세를 그대로 전달
- 같은 세션 안에서 "Generator 모드로 구현" 같은 롤플레이 금지

**단, Math Expert 검증 대상 작업은 Math Expert APPROVED 전에는 절대 구현하지 않는다.**

분리 기준은 **파일 수가 아니라 작업 부피와 성격**이다. 다음 중 하나라도 해당되면 분리한다:

- Math Expert 검증을 거친 작업 (검증된 명세를 깨끗한 컨텍스트로 받아 그대로 옮겨야 함)
- 추가/변경되는 코드량이 많은 작업 (대략 100줄 이상이 기준이지만 절대치는 아님)
- 메인 세션 컨텍스트가 이미 무거워진 상태 (긴 대화, 여러 파일 탐색 후)
- 같은 패턴을 반복적으로 적용하는 작업 (sonnet으로 비용 절감 가능)
- 새 클래스/모듈을 통째로 작성하는 작업 (파일 수가 1개여도 부피가 큼)

다음 경우는 메인 세션이 직접 처리한다:

- 짧은 변경 (한 함수 내부 수정, 몇 줄 추가, 시그니처 정리)
- 즉시 다음 행동을 막는 핵심 결정과 얽힌 작업 (인터페이스 설계 분기 등)
- 대화 맥락이 코드에 직접 필요한 작업 (사용자가 방금 보여준 예시를 반영해야 하는 경우 등)
- subagent 호출 오버헤드가 작업 자체보다 큰 경우

원칙은 동일하다:
- Planner가 고정한 범위와 완료 기준을 바꾸지 않는다.
- Math Expert APPROVED 수식을 임의로 변형하지 않는다 (수학적 동치라도 형태 유지).
- 문서나 설정 파일의 의미가 달라졌다면 관련 문서를 함께 맞춘다.
- 기존 코드 스타일을 따른다 (.NET Framework 4.8 호환, `== false` 선호, 새로 도입된 C# 기능은 코드베이스에 이미 쓰이고 있는 것만 사용).
- 최소 surgical 변경을 우선한다. 큰 리팩터링은 별도 작업으로 분리한다.

Generator는 자의적으로 범위를 확장하지 않는다. "이 김에 이것도 고치면 좋겠는데" 라는 충동은 Planner로 다시 보내는 신호다.

작업 중 다음을 발견하면 Generator는 즉시 멈추고 메인 세션에 보고한다:
- 변경 범위가 Planner가 정한 것보다 커야 한다고 판단될 때
- 새로운 수식이 추가로 필요해질 때 (Math Expert 재검토 트리거)
- 기존 동작과 충돌하는 호출처를 발견했을 때
- 인터페이스 설계 분기가 필요할 때

## Evaluator

구현 후 반드시 수행하는 별도 검토 단계. **반드시 Task 도구로 `evaluator` subagent를 호출한다.** 정의는 `agents/evaluator.md`를 따른다.

호출 방식:
- Task 도구 invoke → `subagent_type: evaluator`
- prompt에 변경된 파일 목록 + Planner 범위 + (해당 시) Math Expert APPROVED 명세를 전달
- 같은 세션 안에서 "Evaluator 관점으로 검토" 같은 롤플레이 금지. 본인이 짠 코드를 본인이 자기 출력으로 검증하는 것은 검증이 아니다.

핵심 질문은 "돌아가는가"가 아니라 **"운영 중 깨질 지점이 남아 있는가"** 이다.

확인 항목:
- 회귀 위험 (기존 동작이 깨졌을 가능성)
- 민감정보 노출 (로그, 에러 메시지, 응답)
- 실거래 부작용 가능성 (테스트 환경에서 검증되지 않은 경로)
- Math Expert 검증 결과와 실제 구현이 일치하는가
- 경계 조건이 코드에 반영되어 있는가
- 단위가 일관되게 사용되었는가

Evaluator는 구현자와 다른 관점에서 본다. 본인이 짠 코드라도 한 번은 "처음 보는 사람" 모드로 점검한다.

## Subagent 분리 가이드

분리 결정은 **파일 수가 아니라 작업 부피와 성격**으로 한다. 분리하기로 결정했다면 **반드시 Task 도구로 호출**한다. 같은 세션에서 역할 연기로 흉내내는 것은 금지.

- **단순 변경** (Math Expert 불필요, 짧은 수정): 메인 세션에서 Planner → Generator를 직접 처리해도 된다. Evaluator는 Task 도구로 반드시 호출.
- **수식/리스크 변경 (Math Expert 필요)**: Math Expert는 Task 도구로 호출 필수 (opus). Generator도 Task 도구로 호출해서 검증된 식을 fresh 컨텍스트로 받게 한다 (sonnet). Evaluator도 Task 도구로 호출.
- **부피가 큰 작업**: 파일 수와 무관하게, 추가/변경 코드량이 많거나 새 클래스/모듈을 통째로 작성하는 경우 Generator를 Task 도구로 호출. 메인 세션 컨텍스트 보호와 비용 절감 양쪽에서 이득.
- **컨텍스트가 이미 무거운 상태**: 긴 대화나 여러 파일 탐색 후라면 짧은 작업이라도 Generator를 Task 도구로 분리.

**Task 도구 호출 시 주의:**
- `subagent_type` 필드에 정확한 이름 사용 (`math-expert`, `generator`, `evaluator`)
- `prompt`에 필요한 컨텍스트를 모두 명시적으로 전달 (subagent는 fresh 컨텍스트로 시작하므로 메인 세션이 이미 본 정보를 추측할 수 없음)
- Math Expert APPROVED 결과는 Generator prompt에 그대로 전달. 요약 금지.
- Planner가 정리한 범위/완료 기준/영향 인터페이스는 명시적 텍스트로 전달.

**호출 결과 확인:**
- subagent 호출이 일어나면 사용자 UI에 "Launching agent: ..." 표시가 나타남
- 표시가 안 나타나면 호출이 안 된 것이므로, 같은 세션에서 흉내내지 말고 사용자에게 알린다.

## 흐름이 깨지는 신호

다음이 발생하면 흐름을 멈추고 Planner로 돌아간다:
- Generator가 Planner 범위에서 벗어난 변경을 발견했을 때
- Math Expert가 "검증 불가"를 반환했을 때 (계획에 정보가 부족하다는 뜻)
- Evaluator가 회귀 위험을 발견했을 때
- 작업 도중 새로운 수식/계산이 추가로 필요해졌을 때 (Math Expert 재검토 필요)

## 참조

- `agents/math-expert.md` — Math Expert subagent 정의
- `agents/generator.md` — Generator subagent 정의
- `agents/evaluator.md` — Evaluator subagent 정의
