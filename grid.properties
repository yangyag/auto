# 최소 매수값 = 최하단 슬롯 buy_price (업비트 호가 단위에 맞는 값으로 직접 입력)
MIN_BUY_PRICE=110000000

# 최고 매수값 = 최상단 슬롯 buy_price (업비트 호가 단위에 맞는 값으로 직접 입력)
MAX_BUY_PRICE=126340000

# 시드 시점 현재가 미만 슬롯의 매수합 목표값 KRW
# (총 예산은 이 값을 가중치 비율로 역산해 시스템이 자동 결정)
LOWER_BUDGET_KRW=3000000

# GRID_COUNT 또는 GRID_STEP_PCT 중 정확히 하나만 사용
GRID_STEP_PCT=0.2

# 신규 기본 TP 모델
TP_MODEL=k
TP_K_BASE=9.0
TP_K_FLOOR=7.0
